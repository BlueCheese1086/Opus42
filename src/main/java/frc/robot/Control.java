package frc.robot;

public class Control {
    //not sure what controllers we're using for drive right now, but necessary methods include:
    /*
        getDriveForward (returns a double)
        getDriveTurn (returns a double)
        getSafety
        getIntakeIn
        getIntakeOut
        getIntakeToggle //returns a boolean
        getIndexerIn
        getIndexerOut
        getLauncherShoot
        getClimbUp
        getClimbDown
    */
}
